#include <unistd.h>

int toUpper(int c)
{
	if (c >= 'a' && c <= 'z')
		c -= 32;
	return c;
}

int main(int ac,char **av)
{
	while (ac == 2)
	{
		int i = 0;
		while (av[1][i])
		{
			if 
			if (av[1][i] >= 'A' && av[1][i] <= 'Z')

		}
	}
}
